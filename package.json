{
  "name": "buildanta-ai-institute",
  "private": true,
  "version": "2.0.0",
  "description": "BUILDANTA AI INSTITUTE, Kanpur. React + Vite site, pre-rendered to static HTML.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir dist/server && node scripts/prerender.mjs",
    "preview": "vite preview",
    "check": "node scripts/check-content.mjs"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router": "^7.9.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.1.0"
  },
  "engines": {
    "node": "22.x"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
